# Super Minimal Blog Notes

- Always use the repository virtual environment for Python commands in this project.
- Use `./.venv/bin/python build.py` to build the site.
- Install dependencies with `./.venv/bin/python -m pip install -r requirements.txt`.
- Do not install Python packages into the system/Homebrew interpreter for this repo.